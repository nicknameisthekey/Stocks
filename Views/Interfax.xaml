<UserControl x:Class="Stocks.Views.Interfax"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Stocks.Views"
             xmlns:models="clr-namespace:Stocks.Models"
             mc:Ignorable="d">
    <Control.Resources>
        <DataTemplate DataType="{x:Type models:InterfaxData}">
            <TextBlock>
            <Hyperlink NavigateUri="{Binding Link}" 
                       RequestNavigate="Hyperlink_RequestNavigate" >
               <TextBlock Text="{Binding Text}" TextWrapping="Wrap"/>
            </Hyperlink>
            </TextBlock>
        </DataTemplate>
    </Control.Resources>

    <StackPanel>
        <TextBox Width="200" Height="30" FontSize="18" HorizontalAlignment="Left" Margin="3"
            Text="{Binding SearchString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        <ListView ItemsSource="{Binding FactsList}"
                  MinHeight="300" Width="1000" MaxHeight="700">
        </ListView>
    </StackPanel>
</UserControl>
