<Window x:Class="Stocks.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Stocks"
        mc:Ignorable="d" Height="450" Width="800">
    <Window.Resources>
        <local:PercentConverter x:Key="PercentConveter" />
        <local:ValueToColor x:Key="valueToColor" />
        <DataTemplate  x:Key="lvTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="35"/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Ticker}" Margin="3"></TextBlock>
                <TextBlock Grid.Column="1" Text="{Binding CurrentPrice}" Margin="3"></TextBlock>
                <TextBlock Grid.Column="2" Text="{Binding PriceChangePrcnt, Converter={StaticResource PercentConveter}}"  
                               Foreground="{Binding PriceChange, Converter={StaticResource valueToColor}}"
                               Margin="3">
                </TextBlock>
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ListView ItemsSource="{Binding Prices}" ItemTemplate="{StaticResource ResourceKey=lvTemplate}">
        </ListView>
    </Grid>
</Window>
